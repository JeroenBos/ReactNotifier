export class Parser {
    parseWithInstructions(html: string, f: () => boolean, instructions: any);
}
export class ProcessNodeDefinitions {
    constructor(react: React) {
    }
    readonly processDefaultNode: any;
}
